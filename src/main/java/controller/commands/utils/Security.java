package controller.commands.utils;

import model.entity.User;

public class Security {

/*    public static boolean checkSecurity(SessionRequestContent content, User.ROLE... roles) {
        if (!content.checkSessionAttribute("user")) {
            return false;
        }
        User user = (User) content.getSessionAttribute("user");
        for (User.ROLE role : roles)
            if (user.getUserRole() == role)
                return true;
        return false;
    }*/
}
